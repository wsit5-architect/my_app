from pathlib import Path
import shutil
import subprocess
import sys
import os

from gstreamer_libs import gstreamer_env


def __run(program: str):
    env, runtime_path = gstreamer_env()
    fullpath = shutil.which(program, path=runtime_path)
    if not fullpath:
        raise RuntimeError(f'{program} was not found in {runtime_path}')
    fullpath = str(Path(fullpath).resolve())
    # Restore terminal state on exit if the called program changed the state,
    # such as gst-play
    if os.name == 'posix':
        import termios

        fd = sys.stdin.fileno()
        old_settings = termios.tcgetattr(fd)
    try:
        subprocess.check_call([fullpath, *sys.argv[1:]], env=env)
    except KeyboardInterrupt:
        return 130
    except subprocess.CalledProcessError as e:
        return e.returncode
    finally:
        if os.name == 'posix':
            termios.tcsetattr(fd, termios.TCSADRAIN, old_settings)
    return 0


def gst_device_monitor_1_0():
    return __run('gst-device-monitor-1.0')
def gst_discoverer_1_0():
    return __run('gst-discoverer-1.0')
def gst_inspect_1_0():
    return __run('gst-inspect-1.0')
def gst_launch_1_0():
    return __run('gst-launch-1.0')
def gst_play_1_0():
    return __run('gst-play-1.0')
def gst_shell():
    return __run('gst-shell')
def gst_typefind_1_0():
    return __run('gst-typefind-1.0')
def ges_launch_1_0():
    return __run('ges-launch-1.0')
def gst_dots_viewer():
    return __run('gst-dots-viewer')
def gst_validate_1_0():
    return __run('gst-validate-1.0')
def gst_validate_launcher():
    return __run('gst-validate-launcher')
def gst_validate_media_check_1_0():
    return __run('gst-validate-media-check-1.0')
def gst_validate_rtsp_server_1_0():
    return __run('gst-validate-rtsp-server-1.0')
def gst_validate_transcoding_1_0():
    return __run('gst-validate-transcoding-1.0')
